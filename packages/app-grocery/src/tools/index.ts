// MCP tool registrar for grocery. Pure adapter to the server service layer.
// Zod is the input schema here ONLY — MCP SDK v1 requires it as a peer dep.

// TODO(basile): export const registerGroceryTools: Registrar = (server, ctx) => { ... }

export {};
