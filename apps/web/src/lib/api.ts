// Hono RPC client. Typed end-to-end via the AppType export from apps/server.

// TODO(basile): wire as Hono client is installed.
// import { hc } from "hono/client";
// import type { AppType } from "@onehouse/server/src/composition.ts";
//
// export const api = hc<AppType>(import.meta.env.VITE_API_URL ?? "/");

export {};
