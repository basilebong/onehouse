// Better Auth React client. Passkey + One Tap plugins mirror the server config.

// TODO(basile): wire as better-auth is installed.
// import { createAuthClient } from "better-auth/react";
// import { passkeyClient, oneTapClient } from "better-auth/client/plugins";
//
// export const authClient = createAuthClient({
//   baseURL: import.meta.env.VITE_AUTH_URL,
//   plugins: [
//     passkeyClient(),
//     oneTapClient({ clientId: import.meta.env.VITE_GOOGLE_ID }),
//   ],
// });
//
// export const { signIn, signOut, useSession } = authClient;

export {};
